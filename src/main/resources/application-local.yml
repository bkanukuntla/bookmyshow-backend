server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/world
    username: root
    password: ashburn
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

spring-doc:
  swagger-ui:
    path: /swagger-ui.html